#!/bin/bash
#This program is to try storing output of command to a variable
HOSTNAME_OUTPUT=$(hostname)
echo "This script is running on ${HOSTNAME_OUTPUT}.where ${HOSTNAME_OUTPUT} is the output of the hostname command"
